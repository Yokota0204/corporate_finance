{
  "name": "corporate_finance",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "([ -d ./dist ] && find ./dist -delete || true) && tsc && cp ./appsscript.json ./dist/appsscript.json",
    "push": "clasp push",
    "deploy": "npm run build && npm run push"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/google-apps-script": "^1.0.97",
    "typescript": "^5.8.3"
  }
}
